# CRN Diffusion Model：物理直觉指南

> 用"墨水扩散"类比理解基于化学反应网络（CRN）的扩散模型

---

## 目录

1. [核心类比：墨水与水箱](#1-核心类比墨水与水箱)
2. [三个关键参数的物理意义](#2-三个关键参数的物理意义)
3. [前向过程：墨水如何扩散](#3-前向过程墨水如何扩散)
4. [反向过程：从噪声还原图像](#4-反向过程从噪声还原图像)
5. [参数选择的物理约束](#5-参数选择的物理约束)
6. [与高斯扩散模型的对比](#6-与高斯扩散模型的对比)

---

## 1. 核心类比：墨水与水箱

想象一个装满清水的水箱，向其中滴入一滴墨水。

- 墨水分子在水中随机游走（布朗运动）
- 同时，水箱边界不断向内补充清水（稀释效应）
- 最终，墨水均匀分布在整个水箱中

**CRN 扩散模型做的事情完全相同**——只不过"墨水"是图像像素值，"水箱"是概率空间。

<!-- 图片占位：墨水扩散示意图（左：初始状态，右：平稳状态） -->
![墨水扩散示意图](images/ink_diffusion_analogy.png)

---

## 2. 三个关键参数的物理意义

### 2.1 $v_0$：水箱的大小

$$v_0 \;\longleftrightarrow\; \text{水箱体积}$$

| $v_0$ 小（如 10） | $v_0$ 大（如 100） |
|-----------------|-----------------|
| 水箱小，墨水浓度波动大 | 水箱大，浓度波动小 |
| Poisson 噪声显著 | 接近高斯分布 |
| 高斯近似误差大 | 高斯近似精确 |
| 生成图像偏向稀疏类别 | 类别分布均匀 |

**数学表达**：归一化浓度 $x = n/v_0$，其中 $n$ 为粒子数。大 $v_0$ 极限下：

$$\sigma^2(t) = \frac{(1-e^{-t})(x_0 e^{-t}+1)}{v_0} \;\xrightarrow{v_0 \to \infty}\; 0$$

<!-- 图片占位：不同 v0 下的生成样本对比（v0=10 vs v0=100） -->
![v0 对比](images/v0_comparison.png)

### 2.2 $T$：扩散停止时间

$$T \;\longleftrightarrow\; \text{等待墨水扩散的时间}$$

- 等待时间越长，墨水越均匀，越接近平稳分布
- 但等太久没有意义——墨水已经完全混合后再等也不会更均匀

**判断标准**：信号保留比 $e^{-T}$

| $T$ | $e^{-T}$ | 物理含义 |
|-----|----------|---------|
| 1.0 | 0.368 | 原始信号保留 37%，扩散不充分 |
| 3.0 | 0.050 | 原始信号保留 5%，基本混合 |
| 5.0 | 0.007 | 原始信号保留 0.7%，充分混合 |
| 8.0 | 0.0003 | 原始信号保留 0.03%，过度等待 |

**实践建议**：$T = 5$ 是最优选择——充分混合（$e^{-T} < 1\%$）且不浪费训练预算。

<!-- 图片占位：不同 T 下的 loss 曲线对比 -->
![T 对比](images/T_comparison_loss.png)

### 2.3 $T_{\min}$：最早观测时刻

$$T_{\min} \;\longleftrightarrow\; \text{最早能观测到的时刻}$$

- 刚滴入墨水的瞬间（$t \approx 0$），浓度极不均匀，方差极大
- 模型在这个时刻很难学到有用的信号
- $T_{\min}$ 是训练时跳过的"太早"时刻

**物理约束**：$T_{\min} \gg 1/v_0$

| $v_0$ | $1/v_0$ | 推荐 $T_{\min}$ |
|--------|---------|----------------|
| 10 | 0.1 | 0.1 ~ 0.2 |
| 100 | 0.01 | 0.05 ~ 0.1 |
| 255 | 0.004 | 0.02 ~ 0.05 |

<!-- 图片占位：T_min 对训练信号的影响示意图 -->
![T_min 示意图](images/T_min_signal.png)

---

## 3. 前向过程：墨水如何扩散

### 3.1 化学反应方程

CRN 由两个基本反应构成：

$$\emptyset \xrightarrow{v_0} X \qquad \text{（清水补充：出生反应）}$$

$$X \xrightarrow{1} \emptyset \qquad \text{（墨水消散：死亡反应）}$$

- 出生速率 $v_0$：水箱越大，补充清水越快
- 死亡速率 1：墨水分子以固定速率消散

### 3.2 精确转移核

给定初始浓度 $x_0$，$t$ 时刻的浓度分布为：

$$\boxed{x_t \mid x_0 \;\sim\; \frac{1}{v_0}\left[\underbrace{\mathrm{Poisson}(v_0 x_0 e^{-t})}_{\text{存活的原始墨水}} \;+\; \underbrace{\mathrm{Poisson}(v_0(1-e^{-t}))}_{\text{新补充的清水}}\right]}$$

**物理解读**：
- 第一项：原始墨水分子以概率 $e^{-t}$ 存活（指数衰减）
- 第二项：新补充的"背景"粒子（平稳分布贡献）

### 3.3 平稳分布（$t \to \infty$）

$$x_\infty \;\sim\; \frac{\mathrm{Poisson}(v_0)}{v_0}, \qquad \mathbb{E}[x_\infty] = 1$$

墨水完全混合后，浓度均值为 1，方差为 $1/v_0$。

<!-- 图片占位：前向过程可视化（x0 → x_T 的轨迹） -->
![前向过程轨迹](images/forward_process_trajectory.png)

---

## 4. 反向过程：从噪声还原图像

### 4.1 Score Function（条件动量）

反向过程的核心是学习"梯度方向"——告诉模型如何从当前状态 $x_t$ 推断原始图像 $x_0$：

$$p_t = \nabla_{x_t} \log Q_t(x_t \mid x_0) = -\frac{x_t - \mu(t)}{\sigma^2(t)}$$

其中：

$$\mu(t) = x_0 e^{-t} + (1 - e^{-t}), \qquad \sigma^2(t) = \frac{(1-e^{-t})(x_0 e^{-t}+1)}{v_0}$$

**物理解读**：$p_t$ 告诉我们当前浓度 $x_t$ 偏离"期望位置" $\mu(t)$ 多远，以及这个偏差有多显著（除以方差归一化）。

### 4.2 反向采样步骤

**Step 1**：U-Net 预测当前时刻的动量

$$\hat{p}_t = f_\theta(x_t, t)$$

**Step 2**：从动量反解原始图像估计

$$\hat{x}_0 = \frac{\hat{p}_t(1-e^{-t})/v_0 + x_t - 1 + e^{-t}}{e^{-t}(1 - \hat{p}_t(1-e^{-t})/v_0)}, \quad \hat{x}_0 \in [0, 1]$$

**Step 3**：从估计的 $\hat{x}_0$ 采样下一步

$$x_{t-\Delta t} = \frac{\mathrm{Poisson}(v_0\,\hat{x}_0\,e^{-(t-\Delta t)}) + \mathrm{Poisson}(v_0(1 - e^{-(t-\Delta t)}))}{v_0}$$

<!-- 图片占位：反向采样轨迹可视化（x_T → x_0 的去噪过程） -->
![反向采样轨迹](images/reverse_sampling_trajectory.png)

---

## 5. 参数选择的物理约束

### 5.1 三个约束条件

```
约束 1：先验匹配
    e^{-T} < 0.01  →  T > 4.6
    确保 x_T 已充分混合至平稳分布

约束 2：高斯近似有效
    v_0 · A_t ≫ 1，其中 A_t = (1 - e^{-t})
    确保 Poisson 分布可用高斯近似

约束 3：训练信号充足
    T_min ≫ 1/v_0
    避免在方差极大的早期时刻浪费训练预算
```

### 5.2 推荐参数组合

| 数据集 | $v_0$ | $T$ | $T_{\min}$ | 说明 |
|--------|--------|-----|------------|------|
| MNIST | 100 | 5.0 | 0.05 | 平衡精度与效率 |
| CIFAR-10 | 255 | 5.0 | 0.02 | 对应 8-bit 像素 |
| CIFAR-100 | 255 | 10.0 | 0.2 | 更长扩散时间 |

<!-- 图片占位：参数空间示意图（v0-T 平面上的有效区域） -->
![参数空间](images/parameter_space.png)

### 5.3 训练信号分析

有效训练区间 $[T_{\min}, T^*]$ 中，信息量随时间的分布：

$$\text{信息量} \propto \frac{1}{\sigma^2(t)} = \frac{v_0}{(1-e^{-t})(x_0 e^{-t}+1)}$$

- 小 $t$（$t < T_{\min}$）：方差极大，信噪比低，跳过
- 中 $t$（$T_{\min} < t < 2$）：信息最丰富，模型学习最有效
- 大 $t$（$t > 2$）：接近平稳分布，score 趋于零，信息稀少

<!-- 图片占位：训练信号密度随时间的分布图 -->
![训练信号分布](images/training_signal_density.png)

---

## 6. 与高斯扩散模型的对比

| 维度 | 高斯扩散（DDPM） | CRN 扩散 |
|------|----------------|---------|
| 噪声类型 | 连续高斯噪声 | 离散 Poisson 噪声 |
| 平稳分布 | $\mathcal{N}(0, 1)$ | $\mathrm{Poisson}(v_0)/v_0$ |
| 物理对应 | 布朗运动 | 化学反应网络 |
| 像素约束 | 无（可为负） | 自然非负（粒子数 ≥ 0） |
| 参数含义 | 抽象噪声调度 | 物理可解释（体积、时间） |
| 大 $v_0$ 极限 | — | 退化为高斯扩散 |

**核心优势**：CRN 框架天然保证像素非负性，且参数具有明确的物理意义，便于理论分析和参数调优。

<!-- 图片占位：CRN vs DDPM 生成样本对比 -->
![CRN vs DDPM](images/crn_vs_ddpm_comparison.png)

---

## 附：数学符号速查

| 符号 | 含义 |
|------|------|
| $v_0$ | 系统体积（水箱大小），控制噪声精度 |
| $T$ | 扩散总时间（等待时间上限） |
| $T_{\min}$ | 最小训练时间（跳过极早期） |
| $x_0$ | 原始图像像素值 $\in [0, 1]$ |
| $x_t$ | $t$ 时刻的加噪图像 |
| $n_t = v_0 x_t$ | 粒子数（整数） |
| $\mu(t)$ | 前向过程均值 |
| $\sigma^2(t)$ | 前向过程方差 |
| $p_t$ | 条件动量（score function） |
| $e^{-t}$ | 信号保留比（衰减因子） |

---

*基于 [CRN-based Diffusion Models](.) 项目 | 数据集：MNIST | 日期：2026-05*
